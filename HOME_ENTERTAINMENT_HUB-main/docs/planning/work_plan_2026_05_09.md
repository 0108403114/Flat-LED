# 本日の作業計画

作成日: 2026-05-09
対象: HOME_ENTERTAINMENT_HUB Phase G (MVPサンプル完成に向けた仕上げ)

## 本日のMVPスコープ

- READY と LIVE に専念する
- NAV は本日スコープ外
- HOME モードは別スコープ

## 今日のゴール

1. TouchDesigner 操作のみで 4 シーン切替・輝度変更・歌詞送信・HB 維持が安定動作する状態にする。
2. 30 分連続運転で破綻がないことを再確認する。
3. 結果を計画/進捗資料へ反映し、次回の再開時間を短縮する。

## 完了条件 (Done)

- [ ] LIVE / HEALING / ENTRY / OFF の連続切替が成功する
- [x] DIMMER が意図どおり反映される
- [x] 歌詞送信が意図どおり反映される
- [ ] HB が連続運転中に停止しない
- [ ] 最後に OFF で安全終了する
- [ ] 今日の記録を docs に反映する

## 5時間タイムボックス

### 1) 00:00-00:30 環境立ち上げ・事前チェック

実施:
- TD 接続確認
- controller 初期化状態確認
- HB 状態確認
- ノード配置の目視確認

終了条件:
- [x] `td_led_mcp_client.py status` が成功
- [x] 最低 1 回のシーン送信が成功

### 2) 00:30-01:30 基本操作の通し確認

実施:
- 4 シーン切替を順次実行
- DIMMER 段階変更
- 歌詞送信を複数回実行

終了条件:
- [x] 主要操作で失敗 0 回
- [x] 再現手順が固定化

### 3) 01:30-02:30 HB 運用と復旧性確認

実施:
- HB 開始/停止/再開
- 再初期化が必要なケースの確認

終了条件:
- [x] 期待手順どおりに復旧できる
- [x] 手動対応が必要な分岐を特定済み

### 4) 02:30-03:00 休憩 + 中間判定

実施:
- ログ確認
- 不具合候補の優先順位付け

終了条件:
- [ ] 後半で対応する項目を確定

### 5) 03:00-04:00 30分安定試験 (本番想定)

実施:
- HB 維持
- 一定間隔でシーン/DIMMER/歌詞を更新

本日の判断:
- [x] SKIP（次回セッションへ繰り越し）

終了条件:
- [ ] 試験中断なし（次回）
- [ ] 致命的エラーなし（次回）

### 6) 04:00-04:40 手直し + 短時間再試験

実施:
- 試験で出た課題を優先順で修正
- リグレッション確認

終了条件:
- [ ] 修正箇所の再確認完了

### 7) 04:40-05:00 成果整理・終了処理

実施:
- 進捗/計画/notebooklm の差分更新
- 次回開始手順を追記
- OFF で安全終了

終了条件:
- [ ] 記録更新完了
- [ ] 安全終了完了

## 実行コマンド (必要時)

```bash
# 状態確認
python tools/td_led_mcp_client.py status
python tools/td_hb_setup.py check

# HB 起動
python tools/td_hb_setup.py

# シーン切替
python tools/td_led_mcp_client.py scene LIVE
python tools/td_led_mcp_client.py scene HEALING
python tools/td_led_mcp_client.py scene ENTRY
python tools/td_led_mcp_client.py scene OFF

# 輝度・歌詞
python tools/td_led_mcp_client.py dimmer 180
python tools/td_led_mcp_client.py lyrics "TEST LINE"
```

## メモ

- 想定外エラーが出たら、まず `status` と `check` を再実行して controller/hb の状態を確認する。
- TD 再起動後に controller が未初期化なら `python tools/td_led_mcp_client.py init` を実行する。

## 進捗ログ (当日)

- 16:57: `status` 確認。controller=initialized、hb_thread=stopped。
- 16:58: `td_hb_setup.py` で HB 起動。hb_thread_running=True。
- 16:58: `scene LIVE` 送信成功。
- 16:59: `scene HEALING/ENTRY/OFF`、`dimmer 180`、`lyrics "MVP TEST LINE"` 送信成功。
- 17:00: HB stop->check->start->check を実施し、復旧手順の成立を確認。
- 17:05: 30分安定試験は本日 SKIP と判断。HB 停止 + scene OFF で安全状態に移行。
